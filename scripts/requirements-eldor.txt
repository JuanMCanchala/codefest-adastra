# Dependencias del precálculo ELDOR (scripts/eldor_precalcular.py).
# No van en la imagen del agente: en tiempo de respuesta solo se leen los JSON de
# agent/datos/eldor/, sin torch ni transformers.
#
# Versiones con las que se corrió el precálculo del 19-sep-2026. El remapeo de claves
# de app/eldor/modelo.py apunta al layout de SegFormer de transformers 5.x; con 4.x
# sobra (las claves del checkpoint ya son las de esa versión).
torch==2.10.0
transformers==5.13.1
huggingface-hub==1.28.0
pillow==11.2.1
numpy==2.2.5

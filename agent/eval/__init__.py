"""Banco de evaluación del agente (Parte 1). Aislado de ``app``: solo consume el
contrato §2.4 por HTTP, así que sirve igual contra el agente local, el servidor
offline y el endpoint desplegado en Coolify."""

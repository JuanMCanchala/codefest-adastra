# Versiones exactas verificadas en la imagen del agente (18-sep-2026). Se fijan para que
# el build en Coolify instale lo mismo que se probó en local: un cambio de una
# dependencia transitiva puede dejar sin cargar el clasificador de inyección o el
# reranker, y ese fallo solo se vería en el log.
# torch se instala aparte en el Dockerfile (build de CPU).

# Servicio HTTP
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
# Orquestación multiagente
langgraph==1.2.11
# Amazon Bedrock nativo (alternativa al gateway de ADL)
boto3==1.43.98
# Recuperación (base vectorial de la Etapa 1) y clasificador de inyección
faiss-cpu==1.15.1
FlagEmbedding==1.4.2
sentence-transformers==6.1.0
transformers==5.17.0
tokenizers==0.23.2
pyyaml==6.0.3
networkx==3.6.1
